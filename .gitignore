# dependencies
node_modules/

# build output
dist/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# editor/OS
.DS_Store
.vscode/
*.local

# env files (never commit secrets)
.env
.env.local

# linksman-api deploys straight to InterWorx hosting, not through git/Vercel —
# config.php holds real DB credentials and must never be committed
linksman-api/config.php
